<?php
/**
 * @param $existing
 * @param $type
 * @param $theme
 * @param $path
 *
 * @return array
 */
function event_lister_theme($existing, $type, $theme, $path) {
  return array('event_lister' =>
    array(
      'variables' => array(
        'events' => NULL,
      ),
      'template' => 'block--event-lister'
    )
  );
}